name: medical_examination_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  dartz: ^0.10.1
  dio: ^5.4.3+1
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.1.0
  flutter_secure_storage: ^9.2.2
  font_awesome_flutter: ^10.7.0
  google_fonts: ^6.2.1
  internet_connection_checker: ^1.0.0+1
  provider: ^6.1.2
  shared_preferences: ^2.2.3
  speech_to_text: ^6.6.0
  tiengviet: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/.env
    - assets/
    - assets/images/
    - assets/icons/
    - assets/fonts/
